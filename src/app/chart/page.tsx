import { PlaceholderPage } from "@/components/placeholder-page";

export default function ChartPage() {
  return (
    <PlaceholderPage
      title="The Chart"
      subtitle="A pipeline map of every voyage by stage."
      note="The deal map (Kanban across the VoyageStage columns) lands in a sibling issue."
    />
  );
}
