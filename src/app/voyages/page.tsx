import { PlaceholderPage } from "@/components/placeholder-page";

export default function VoyagesPage() {
  return (
    <PlaceholderPage
      title="Voyages"
      subtitle="Deals in flight, charted through the stages."
      note="The voyage board lands in a sibling issue. Seeded voyages span every stage already."
    />
  );
}
