import { PlaceholderPage } from "@/components/placeholder-page";

export default function LogPage() {
  return (
    <PlaceholderPage
      title="The Log"
      subtitle="A timeline of every activity across the fleet."
      note="The activity timeline lands in a sibling issue. Seeded activities are ready to render."
    />
  );
}
